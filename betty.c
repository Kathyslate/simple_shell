#include <stdio.h>

/**
 * add - adds 2 integers
 * @a: first integer
 * @b: second integer
 *
 * Description: 'a program that accepts two integers and sum them up'
 *
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
